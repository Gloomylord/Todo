import React from "react";

const Header: React.FC = () => {
    return <nav className='header'>
            <div className="nav-wrapper px1 teal lighten-1">
                <a href="#" className="brand-logo center">Todo with react and TS</a>
            </div>
    </nav>
};

export default Header;
